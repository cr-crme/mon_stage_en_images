enum Target { none, individual, all }

enum QuestionType { photo, text, any }

enum AnswerStatus {
  deactivated,
  needStudentAction,
  needTeacherAction,
  validated
}

enum LoginType { teacher, student }
