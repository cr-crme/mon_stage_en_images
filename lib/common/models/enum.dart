enum Target { none, individual, all }

enum ActionRequired {
  none,
  fromStudent,
  fromTeacher,
}

enum LoginType { none, teacher, student }

enum QuestionView {
  normal,
  modifyForOneStudent,
  modifyForAllStudents,
}
